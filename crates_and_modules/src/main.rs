fn main() {
    println!("Hello, world!");
}

// Crate: `cargo build --verbose` to build a project with
// Delete compiled code: cargo clean
// cargo build --release [profile.release]

// Profiler: A tool that measures where your program is spending its CPU time.
// 